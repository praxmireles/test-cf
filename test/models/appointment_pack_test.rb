# frozen_string_literal: true

require 'test_helper'

describe AppointmentPack do
  let(:appointment_package) { AppointmentPack.new }

  # it 'must be valid' do
  #   value(appointment_package).must_be :valid?
  # end
end
