# frozen_string_literal: true

require 'test_helper'

describe SeniorityLevel do
  let(:seniority_level) { SeniorityLevel.new }

  # it 'must be valid' do
  #   value(seniority_level).must_be :valid?
  # end
end
