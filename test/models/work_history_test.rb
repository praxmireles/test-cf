# frozen_string_literal: true

require 'test_helper'

describe WorkHistory do
  let(:work_history) { WorkHistory.new }

  # it 'must be valid' do
  #   value(work_history).must_be :valid?
  # end
end
